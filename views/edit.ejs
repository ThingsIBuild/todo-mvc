<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Todo</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <h1 class="text-2xl font-bold text-center my-4">Edit Todo</h1>
    <main class="container mx-auto max-w-2xl">
        <form action="/api/v1/todos/<%= todo._id %>" method="POST"
            class="flex flex-col gap-4 bg-white p-6 rounded-lg shadow ">
            <input type="text" name="title" value="<%= todo.title %>" required
                class="w-full p-2 border border-gray-300 rounded focus:ring-2 focus:ring-blue-400">
            <textarea name="description" required
                class="w-full p-2 border border-gray-300 rounded focus:ring-2 focus:ring-blue-400"><%= todo.description %></textarea>
            <button type="submit"
                class="w-full bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded transition">Update
                Todo</button>
                <a href="/" class="text-blue-500 hover:text-blue-700 font-medium transition">back to todos</a>
        </form>
    </main>
</body>

</html>